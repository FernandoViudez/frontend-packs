export const enum IntegratedWallet {
  myAlgo = 'myAlgo',
  peraWallet = 'peraWallet',
}

export const INTEGRATED_WALLETS = Object.freeze({
  myAlgo: IntegratedWallet.myAlgo,
  peraWallet: IntegratedWallet.peraWallet,
});
